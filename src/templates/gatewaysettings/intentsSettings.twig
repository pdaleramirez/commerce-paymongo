{% from "_includes/forms" import textField, lightswitchField, selectizeField, autosuggestField %}


{{ selectizeField({
  id: 'paymentType',
  id: 'paymentType',
  label: 'Payment Type'|t('commerce-paymongo'),
  name: 'paymentGatewayType',
  options: [
    { value: 'card', label: 'Card' },
    { value: 'gcash', label: 'GCASH' },
  ],
  value: gateway.paymentGatewayType,
  errors: gateway.getErrors('paymentGatewayType')
}) }}

{{ autosuggestField({
  label: 'Api Key'|t('commerce-paymongo'),
  id: 'apiKey',
  class: 'ltr',
  name: 'apiKey',
  suggestEnvVars: true,
  value: gateway.apiKey,
  errors: gateway.getErrors('commerce-paymongo')
}) }}

{{ autosuggestField({
  label: "Secret"|t('commerce-paymongo'),
  id: 'secret',
  class: 'ltr',
  name: 'secret',
  suggestEnvVars: true,
  value: gateway.secret,
  errors: gateway.getErrors('secret')
}) }}